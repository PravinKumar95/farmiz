{
  "name": "poultry-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "start.cmd",
    "tailwind": "npx @tailwindcss/cli -i ./input.css -o ./assets/tailwind.css --watch",
    "serve": "dx serve"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
