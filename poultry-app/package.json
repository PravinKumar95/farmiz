{
  "name": "poultry-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "start.cmd",
    "tailwind": "npx @tailwindcss/cli -i ./input.css -o ./assets/tailwind.css --watch",
    "serve": "dx serve",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@tailwindcss/cli": "^4.0.0",
    "dotenv": "^17.4.2",
    "tailwindcss": "^4.0.0"
  }
}
